# 사용 방법 - Antigravity Engine

## 🚀 실행 방법

### 1단계: API 키 설정

`config.json`에서 Google Gemini API 키만 입력하세요:

```json
{
  "google_api_key": "실제_API_키를_입력하세요"
}
```

API 키 받기: https://makersuite.google.com/app/apikey

### 2단계: 실행

**`run.command`** 더블클릭 또는:

```bash
./run.command
```

### 3단계: 인터랙티브 입력

**📂 파일 선택**
- 파일 선택 다이얼로그가 자동으로 열립니다
- 변환할 이미지(.jpg, .png, .webp) 또는 CSV(.csv) 파일을 선택하세요

**🎨 스타일 선택**
- 원하는 디자인 스타일을 선택하세요 (1-5):
  1. Cyberpunk Neon
  2. Corporate Professional
  3. Minimalist Modern
  4. Nature Organic
  5. Custom (직접 입력)

**✨ 완료!**
- 대시보드가 자동으로 생성됩니다
- `{파일명}_dashboard.html` 파일을 브라우저로 열어보세요

---

## 💡 고급 사용법

### 명령줄에서 직접 실행

```bash
# 인터랙티브 모드 (기본)
python src/antigravity_engine.py --interactive

# 또는 짧게
python src/antigravity_engine.py -i
```

### config.json 사용 모드

파일과 스타일을 미리 설정하고 싶다면:

```json
{
  "google_api_key": "실제_API_키",
  "target_file": "data/raw/your_file.csv",
  "design_style": "원하는 스타일 설명"
}
```

그리고 플래그 없이 실행:
```bash
python src/antigravity_engine.py
```

---

## 🎨 디자인 스타일 프리셋

인터랙티브 모드에서 제공되는 스타일:

1. **Cyberpunk Neon**
   - 어두운 배경 (#0a0e27)
   - 핑크 (#ff006e) & 시안 (#00f5ff)
   - 글리치 효과, 네온 글로우

2. **Corporate Professional**
   - 깔끔한 흰색 배경
   - 네이비 블루 (#1e3a8a)
   - 은은한 그림자, 둥근 모서리

3. **Minimalist Modern**
   - 화이트 배경
   - 라이트 그레이 강조
   - 최대 여백, 심플

4. **Nature Organic**
   - 어스톤 컬러
   - 녹색 & 갈색 팔레트
   - 부드러운 곡선

5. **Custom**
   - 직접 원하는 스타일 입력
   - 예: "Dark purple background, Gold accents, Elegant serif fonts"
